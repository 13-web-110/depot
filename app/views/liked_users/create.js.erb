<% if session[:user_id] == nil %>
	alert("Please login in first.")
<% else %>
	<% if @cur_liked_user_exist == false %>
		$('#liked_user_<%= @product.id %>').html("<%=j link_to t('.like') +  "  (" + @product.liked_users.count.to_s + ")" , liked_users_path(product_id: @product.id),:method => :post ,remote:true %> ")
	<% else %>
		alert("You have liked it.")
	<% end %>
<% end %>
