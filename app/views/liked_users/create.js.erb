<% if session[:user_id] == nil %>
	alert("Please login in first.")
<% else %>
		$('#liked_user_<%= @product.id %>').html("<%=j link_to t('.cancer_like') +  "  (" + @product.liked_users.count.to_s + ")" ,:controller => 'liked_users',:action => 'cancer_like',:product_id =>@product.id, remote: true %> ")
<% end %>
