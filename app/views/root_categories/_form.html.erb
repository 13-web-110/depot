<%= form_for(@root_category, :html => {:multipart=> true}) do |f| %>
  <% if @root_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@root_category.errors.count, "error") %> prohibited this root_category from being saved:</h2>

      <ul>
      <% @root_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br />
    <%= f.file_field :image_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
